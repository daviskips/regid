let firstName="dave"
let secondName="singoei"
let age=30
let address="nairobi"

let userDetails=firstName+" "+secondName+" is "+age+" years old and lives in "+address
console.log(userDetails)
console.lobg("user details is",typeof userDetails)

//tempelate string use backticks
let userDetails2=`${firstName} ${secondName} is ${age} years old and lives in ${address}`
console.log(userDetails2)
console.log(`${firstName} ${secondName} is ${age} years old and lives in ${address}`)
